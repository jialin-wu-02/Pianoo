import React, { Component } from 'react';
import classes from './BlackKey.module.css';

class BlackKey extends Component {
  
  render() {
    return (
      <div className={classes.BlackKey}>
      	<button> A black key. </button>
      </div>
    );
  }
}

export default BlackKey;
